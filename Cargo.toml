[package]
name = "Countdown"
version = "0.1.0"
edition = "2024"

[build-dependencies]
winres = "0.1"

[dependencies]
clap = {version = "4.5.47",features = ["derive"]}

[dependencies.sdl2]
version = "0.38"
default-features = false
features = ["image","ttf"]

